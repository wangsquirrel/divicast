# 六爻模块当前功能说明

本文档描述 `divicast` 当前六爻排盘模块已经实现的能力、输出范围、测试覆盖和已知边界。
内容以当前代码为准，不再保留旧版“变更记录”式描述。

## 模块定位

六爻模块的目标是提供一套可编程调用的六爻排盘能力，适合作为：

- Python 业务代码中的排盘底层库
- MCP / API 的排盘与结构化输出依赖
- 卦盘展示、检索、对照分析的基础能力层

当前模块更偏向“结构化排盘 + 稳定输出”，其中：

- 起卦时间、四柱、月建、日辰、空亡等属于时间事实层
- 本卦、变卦、六亲、六神、世应、伏神等属于卦盘装配层
- 神煞、卦身、香闺、床帐等属于补充展示信息

当前模块不包含分析层，重点是把六爻盘面本身完整输出出来。

## 代码结构

六爻相关代码位于：

- `src/divicast/sixline/divinatory_symbol.py`
- `src/divicast/sixline/casting.py`（输入归一化、校验与来源记录）
- `src/divicast/sixline/output.py`
- `src/divicast/time_utils.py`（按次调用的历法规则与时间元数据）

相关基础实体位于：

- `src/divicast/entities/trigram.py`
- `src/divicast/entities/ganzhi.py`
- `src/divicast/entities/liushen.py`
- `src/divicast/entities/relative.py`
- `src/divicast/entities/daemon.py`
- `src/divicast/entities/wuxing.py`
- `src/divicast/base/symbol.py`

## 已实现的核心能力

### 1. 起卦入口

当前提供的主入口是：

```python
DivinatorySymbol.create(
    cnts=None, now=None, bazi=None,
    *, line_values=None, coin_counts=None, coin_side=None, calc_rules=None,
)
```

已经实现：

- 支持直接传入 6 次摇卦结果
- 不传摇卦结果时自动随机生成
- 支持显式传入起卦时间
- 支持显式传入八字
- 不传八字时根据时间自动生成起卦四柱

三种记录入口互斥，顺序统一为初爻到上爻：

| 输入 | 含义 |
| --- | --- |
| `cnts` | 旧版编码：0 老阴、1 少阳、2 少阴、3 老阳，保持既有盘面兼容 |
| `line_values` | 标准值：6 老阴、7 少阳、8 少阴、9 老阳；推荐新调用使用 |
| `coin_counts` + `coin_side` | 传统铜钱计数；`text` 字面或 `back` 背面必须显式指定 |

传统铜钱法按字面计 2、背面计 3 相加；字面数 0/1/2/3 对应标准值 9/8/7/6，
背面数则对应 6/7/8/9。旧文档把 `cnts`／`yaogua` 称为字面数有误；不能因此
自动翻转历史记录。现代硬币应先明确字面与背面的对应，不作猜测。

只在三种记录都为 `None` 时模拟三枚公平硬币，四种爻值权重为 1:3:3:1。
空记录、非六项、越界、浮点数、布尔值、互斥参数或未声明计数面均抛出 `ValueError`。
输入被复制为不可变记录，修改调用方的列表不会影响已生成的盘面。

时间约定：

- 只接受 `naive datetime`
- 调用方必须先完成时区、地点、真太阳时等归一化
- 库内不做时区换算和真太阳时修正
- 不传 `now` 时，默认使用当前本地 `naive datetime`
- 完整保留分秒；节气时刻以 tyme 的 UTC+08:00 为基准，调用方负责明确时间基准
- 默认按立春换年、十二节交接换月、23 点换日，不读取或修改全局 `LunarHour.provider`
- `calc_rules={"zi_hour": "default_next_day"}` 为默认规则；
  `lunar_sect2_day_same` 为 Tyme 流派 2，晚子时日柱仍属当天、0 点换日，时柱沿用该流派算法
- 未知规则会报错；共享四柱函数同样按次调用选择日界
- 显式 `bazi` 与 `calc_rules` 互斥。此时元数据标为 `pillar_source=provided`，
  不声称四柱由 `now` 推导，也不编造其日界或节气口径

### 2. 起卦时间四柱

当前已实现：

- 根据起卦时间生成年柱、月柱、日柱、时柱
- 将四柱封装为 `Bazi`
- 将日柱推导为空亡
- 将月柱地支输出为月建
- 将日柱地支输出为日辰

六爻模块中的八字主要用于：

- 排六神
- 定空亡
- 取神煞
- 输出卦盘时间背景

### 3. 本卦与变卦生成

当前已实现：

- 根据 6 爻铜钱结果生成六个爻位
- 生成本卦
- 生成变卦
- 识别内卦与外卦
- 识别卦宫
- 识别六合卦、六冲卦、游魂卦、归魂卦等卦型标签

当前既支持：

- 本卦盘面展示
- 变卦盘面展示
- 结构化输出中同时返回本卦与变卦信息

### 4. 六亲、六神、世应、伏神

当前已实现：

- 安六亲
- 安六神
- 安世应
- 缺亲时补伏神
- 区分本卦爻与变卦爻
- 标注动爻

每个爻位当前可包含：

- 六神
- 本卦爻信息
- 变卦爻信息
- 是否动爻
- 是否世爻
- 是否应爻
- 是否带伏神

### 5. 纳甲、装支、五行

当前已实现：

- 按内卦 / 外卦装天干
- 按卦宫规则装地支
- 计算各爻五行
- 基于本卦所属卦宫五行装六亲

当前这些信息已经足够支持：

- 终端盘面展示
- 结构化接口输出
- 上层系统继续断卦或做规则分析

### 6. 六爻神煞与附加信息

当前已经实现：

- 天乙贵人
- 驿马
- 桃花
- 文昌
- 禄神
- 劫煞
- 华盖
- 将星
- 天喜
- 天医
- 阳刃
- 灾煞
- 谋星

六爻神煞取法采用 `sixline/divinatory_symbol.py` 中的固定规则；不同流派对天乙贵人、天医等可能存在条件变体，当前暂未提供流派配置或变体切换。

此外，还实现了：

- 卦身
- 香闺
- 床帐

这些信息当前主要用于：

- 传统六爻盘面展示
- MCP / API 的结构化返回
- 上层系统对照展示

## 已实现的结构化输出

### 1. 标准输出对象

当前六爻模块的统一输出结构为：

```python
StandardDivinatorySymbolOutput
```

顶层包含：

- `yaogua`
- `line_values`（标准 6–9 爻值）
- `casting`（输入方式、手动原始六值及适用时的计数面）
- `calendar`（实际历法规则或显式四柱来源）
- `time`
- `bazi`
- `yuejian`
- `richen`
- `kongwang`
- `guashen`
- `chuangzhang`
- `xianggui`
- `shensha`
- `benguaming`
- `guagong`
- `bengua_type`
- `bianguaming`
- `biangua_type`
- `yao_1` ~ `yao_6`

`yaogua` 保持旧引擎编码；原始手动输入在 `casting.values`，不能将两者混用。
`casting.input_format` 为 `legacy_yaogua`、`line_values`、`coin_counts` 或
`random_three_coins`；随机起卦不伪造手动输入。`line_values` 可用于复现。

`calendar` 含 `time_basis`、`solar_terms`、`year_boundary`、`month_boundary`、
`day_boundary`、`zi_hour`、`pillar_source`。显式四柱的未知口径为 `None`，
使用 `exclude_none=True` 序列化时省略。读取旧 JSON 时允许缺少新增字段，保持未知，
不会自动补造输入或历法来源。新建盘面总会提供这三项元数据。

模型与随包的 `schema.json` 同步维护；`yaogua` 校验恰好六项合法编码。

### 2. 时间与背景信息

当前已输出：

- 原始摇卦记录
- 起卦时间
- 起卦四柱
- 月建
- 日辰
- 空亡
- 卦身
- 香闺
- 床帐
- 神煞列表

### 3. 本卦 / 变卦信息

当前已输出：

- 本卦卦名
- 本卦卦宫
- 本卦特殊类型
- 变卦卦名
- 变卦特殊类型

### 4. 单爻详细信息

每一爻当前已输出：

- 六神
- 本卦爻六亲
- 本卦爻天干
- 本卦爻地支
- 本卦爻五行
- 本卦爻阴阳线型
- 是否世爻
- 是否应爻
- 是否动爻
- 伏神
- 变卦爻六亲
- 变卦爻天干
- 变卦爻地支
- 变卦爻五行
- 变卦爻阴阳线型

## 已实现的命令行展示能力

当前已实现：

- `rich_draw_divination(ds)`
- `plain_draw_divination(ds)`

这两个函数可以输出：

- 起卦时间背景
- 神煞
- 本卦与变卦
- 六爻逐爻信息
- 动爻、世应、伏神标记

渲染器显示输入协议和阴阳动静。只有真实 `coin_counts` 输入才显示声明过的字面／
背面枚数；旧编码与随机结果不被冒充成用户实际摇出的字背记录。

适合：

- 本地调试
- 命令行查看卦盘
- 示例脚本展示

## 当前依赖

六爻模块当前主要依赖：

- `tyme4py`
- `pydantic`
- `rich`

其中 `tyme4py` 主要用于：

- 根据起卦时间生成四柱
- 提供六爻时间背景所需的干支能力

## 当前测试覆盖

当前六爻模块已经有专门测试覆盖以下内容：

- 六冲卦判定
- 本卦 / 变卦装配
- 六神、六亲、世应、动爻
- 伏神
- 神煞
- 卦身、香闺、床帐
- 标准输出结构
- JSON Schema 校验

主要测试文件包括：

- `tests/test_divinatory_symbol.py`
- `tests/test_sixline_contract.py`：64 卦固定表、八纯卦纳甲、伏神、4096 种动静组合、
  随机权重、边界时刻、输入隔离、按次历法及全局 provider 干扰回归
- `tests/fixtures/sixline_reference.json` 和 `sixline_reference.md`：独立基准及来源说明

此外也有基础实体与时间约束测试，间接支持六爻模块：

- `tests/test_base.py`
- `tests/test_mix.py`

## 当前适合的使用场景

当前六爻模块已经适合：

- 做标准结构化六爻排盘输出
- 驱动卦盘展示页面或终端展示
- 作为 MCP / API 的六爻排盘底层
- 作为上层断卦逻辑、规则分析、搜索系统的输入层

## 当前边界与非目标

以下能力当前没有在库内解决，调用方需要自行处理或接受当前限制：

- 时区自动换算
- 地点经度换算
- 真太阳时修正
- 六爻断卦结论生成
- 用神择取、旺衰裁断、应期判断等高阶分析
- 多流派自由切换的完整公开接口

因此，当前模块更适合作为：

- “排盘事实层”
- “卦盘结构层”

而不是：

- “最终断卦裁决层”

## 对当前状态的简要结论

截至当前代码版本，`divicast` 的六爻模块已经实现了：

- 完整的六爻起卦与本卦 / 变卦生成
- 六亲、六神、世应、伏神、神煞等核心盘面装配
- 标准结构化输出
- 命令行展示
- 基础测试覆盖

整体上，它已经具备“可用的六爻排盘模块”能力，尤其适合做程序化输出和上层系统集成。
如果后续要继续增强，优先方向通常会是：

- 时间口径追踪
- 六爻分析层抽象
- 用神 / 应期 / 断卦规则模块化
- 更完整的神煞与附加术数标签
